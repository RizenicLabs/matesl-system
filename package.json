{
  "name": "matesl-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w api\" \"npm run dev -w web\" \"npm run dev -w ai-service\"",
    "build": "npm run build:deps && npm run build --workspaces --if-present",
    "build:deps": "npm run build -w shared && npm run build -w database",
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint packages/**/*.{ts,tsx,js,jsx}",
    "clean": "npm run clean --workspaces --if-present && rm -rf dist build .next",
    "postinstall": "npm run build:deps"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.38.0",
    "@typescript-eslint/parser": "^8.38.0",
    "concurrently": "^9.2.0",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react": "^7.37.5",
    "eslint-import-resolver-typescript": "^3.6.1",
    "eslint-plugin-import": "^2.29.1",
    "eslint-plugin-prettier": "^5.2.1",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  }
}
