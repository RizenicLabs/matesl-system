{
  "name": "matesl-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=api\" \"npm run dev --workspace=web\" \"npm run dev --workspace=ai-service\"",
    "build": "npm run build:deps && npm run build --workspaces --if-present",
    "build:deps": "npm run build --workspace=shared && npm run build --workspace=database",
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint packages/**/*.{ts,tsx,js,jsx}",
    "clean": "npm run clean --workspaces --if-present && rm -rf dist build .next packages/*/tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.38.0",
    "@typescript-eslint/parser": "^8.38.0",
    "concurrently": "^9.2.0",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react": "^7.37.5",
    "eslint-import-resolver-typescript": "^3.6.1",
    "eslint-plugin-import": "^2.29.1",
    "eslint-plugin-prettier": "^5.2.1",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  }
}
